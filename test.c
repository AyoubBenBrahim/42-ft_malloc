#include "inc/malloc.h"


int main()
{

	void * ptr1 = malloc(10);
    // void * ptr2 = malloc(30);
    // void * ptr4 = malloc(300);

    // free(ptr1);
    // free(ptr2);
    // free(ptr4);

   // show_alloc_mem();

}
